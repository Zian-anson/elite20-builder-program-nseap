# 上游同步记录 - 2026-07-19

## 同步范围

| 上游仓库 | 最新提交 | 提交时间 | 本地同步位置 |
|---|---|---|---|
| `a976xw7td/ai-x-challenge-learning-mvp` | `8e95299603bf6197aeceef01da6fab9333712719` | 2026-07-17 15:49 +08:00 | `platform/ai-x-challenge-learning-mvp/` |
| `a976xw7td/elite20-builder-program-nseap` | `2d9810f79b955c5fc50406ffdcdb8c3ef47967ea` | 2026-07-10 14:46 +08:00 | 项目根级设计目录（无变化，留待下轮） |
| `a976xw7td/nseap-elite20-challenges` | `67e27a7ad8f573e90b0484c890d79924afc90b93` | 2026-07-17 14:48 +08:00 | `challenges/official/` |

## 同步方法

延续 `2026-07-16.md` 的策略：

- 三仓库均 `gh repo clone -- --depth 1` 浅克隆到 `/tmp` 临时目录，不复制上游 `.git/`。
- 用 `rsync -a --delete` 覆盖本地，排除 `node_modules`、`.next`、`.env.local` 等环境与产物。
- 同步后按需补回本地修正、并重装依赖、跑 lint 与 build。
- 设计总部本轮无新增提交，跳过。

## 本轮上游新增重点

### 平台 `ai-x-challenge-learning-mvp`（3 个新提交）

| 提交 | 重点 |
|---|---|
| `7c2bd7b` P0/P1/P2 全面升级 | 新增 `app/(app)/submissions/page.tsx` 提交列表页；重构单条提交详情；教师仪表盘升级；新增 `scripts/integration-test.sh` 端到端集成测试；落地 `public/platform-content.json` 内容外置骨架 |
| `39338e5` 平台内容 API 端点 | 新增 `app/api/platform-content/route.ts`，知识库/文档页改读远程内容；支持 `PLATFORM_CONTENT_URL`（GitHub raw content，5 分钟缓存）；AI 评分改读 GitHub README 全文 + 文件列表 + commit 历史；评分维度改为从飞书 `rubric_dimensions` 动态生成（4 套挑战类型模板）；新增红线正则（硬编码路径、明文 API key）在 LLM 返回值上钳位；新增 `.raymond/plans/` 两份方案文档 |
| `8e95299` README 重写 | 正式确立平台定位为「纯框架」+ 三层校验流水线（GitHub 指针 → 交付物完整性 → AI 初评）+ 角色体系（学生/教师/管理员）+ Agent 化架构（Envelope + Audit Log + Redis Stream 消息总线） |

#### 架构升级关键点

平台评分流水线从「LLM 只听学生自述」升级为「先验证 → 再看代码 → 按结构化规则评分」：

1. **GitHub 指针校验**：仓库存在性、可访问性、README、最新 commit；结果快照写入提交记录。
2. **交付物完整性检查**：从挑战定义 `required_deliverables`（支持 `*通配符*`）读取，缺件直接拦截，**不消耗 AI 调用**。
3. **AI 初评**：发送 README 全文 + 文件列表 + 最新 commit message + 学生自述；按挑战 `rubric_dimensions` 动态维度打分；Zod 运行时校验失败重试一次 fallback。
4. **红线正则检查**：硬编码路径（`/Users/`、`C:\`）、明文 `sk-xxx` API key 在 LLM 返回值上钳位。
5. **路由后续**：按 `review_mode` 分流（教师终审 / 同伴评审 N 人 / 混合），全程飞书 Bot 通知学生 + 班级群。

新增 API 端点：`/api/platform-content`、`/api/submissions`。

### 挑战库 `nseap-elite20-challenges`（1 个新提交）

| 提交 | 重点 |
|---|---|
| `67e27a7` 替换占位符为 Richard 7.14 正式挑战内容 | 13 个挑战 `CHALLENGE.md` 从占位骨架填实为完整正文（C2A/C2G/C3C/C4 系列/C5/C6/C7）；C2G「参数高尔夫」首批附带 3 份讨论材料：`C2G_公众号文章.md`、`C2G_方案草案_模板.md`、`C2G_群消息.md` |

## 本地修正保留

上游 7-17 仍未修两处与 Next.js 15 不兼容的配置项，本地沿用 7-16 的最小修正（rsync 后重新打回）：

| 文件 | 上游 | 本地 | 原因 |
|---|---|---|---|
| `package.json` | `"eslint-config-next": "^16.2.10"` | `"eslint-config-next": "^15.5.19"` | 16.2.10 与 Next 15.5 冲突，安装时报 `ERESOLVE` |
| `.eslintrc.json` | 顶层多余 `"ignoreDuringBuilds": true` | 移除该字段 | `.eslintrc.json` 顶层无 `ignoreDuringBuilds` schema 字段，`next lint` 会忽略；忽略构建期 lint 应写在 `next.config.ts` |

因上游 `package-lock.json` 含 eslint-config-next 16 条目，与本地修正不匹配，`npm ci` 报错。处理方式：删除上游 lockfile，用本地 `package.json` 运行 `npm install` 重新生成 lockfile（指向 `@next/eslint-plugin-next@15.5.19`）。

## 本地保留项

- `challenges/official/README.md` 仍是 7-16 按"整合结构"改写的入口（含项目总览与挑战索引表），上游 README 不同，rsync 时带 `--exclude='README.md'` 保留本地版本。
- 平台业务代码无本地改动，全量跟随上游。

## 验证

| 验证项 | 结果 |
|---|---|
| `npm install`（重建 lockfile） | 通过，150 包，无 ERESOLVE |
| `npm run lint` | 通过，2 个无关警告（`react-hooks/exhaustive-deps`、`no-html-link-for-pages`），均为上游既有 |
| `npm run build` | 通过。新增 `/submissions` 列表页、`/api/platform-content` 端点均成功纳入路由表；`/teacher` 7.89 kB（含仪表盘升级）；共享 First Load JS 102 kB |

## 设计总部开放 issue

`a976xw7td/elite20-builder-program-nseap` 仓库 7-10 后无新提交，但有 5 个 open issue 仍未处理，作为下轮可能合并的素材：

- #7 feat: Add Challenge Scoring System v1.0
- #6 feat(team3): add ontology engineering v1 — 22 W3C-standard files + MVP demo
- #5 feat(platform-team): 提交 learning-platform — Landing Page/LMS/提交流程/作品集
- #4 feat(team3): Agent 本体工程化 + MVP Demo
- #1 Team 6: Add Knowledge Cognitive Cell MVP

## 后续同步方法

延续 7-16 的流程：

1. 读取三仓库最新提交哈希，仅同步发生变化的仓库。
2. 平台仓库更新后至少运行 `npm install`（若 package.json 变更）+ `npm run lint` + `npm run build`，并复核上述两处本地修正是否仍需保留。
3. 挑战仓库更新后检查 `challenges/official/README.md` 是否仍要保留本地版本；若上游 README 已含整合入口则可放回上游版。
4. 设计总部更新后运行本项目结构测试与链接检查。

## 本次同步后本地状态

- 平台：`platform/ai-x-challenge-learning-mvp/` 已至 `8e95299`（+ 本地 2 处修正保留）。
- 挑战：`challenges/official/` 已至 `67e27a7`（含 13 个填实挑战 + C2G 三份讨论材料）；本地 `README.md` 保留。
- 设计总部：仍是 `2d9810f` 基线，无变化。
- 本地无 WIP 待清理；`outputs/`、`tmp/` 维持 7-15/7-16 状态。

## 2026-07-20 cherry-pick 补充

设计总部 5 个 open PR 已完成评审：

| PR | head sha | 处置 | 本地落点 |
|---|---|---|---|
| #1 | `edfc3071aa9459c3e404734e2d5d63d21be5a7bb` | 仅取演进版知识库，排除 docx | `knowledge-base/nseap/` |
| #4 | `0143c5d8a8bb693c721557bc69f8b09cbbff2fab` | 全部 37 个文件 | `teams/agent-team/` |
| #5 | - | 推荐关闭：已被平台仓 `8e95299` 取代 | 不落地 |
| #6 | - | 推荐关闭：与 #4 重复且目录归属分裂 | 不落地 |
| #7 | - | 推荐关闭：二进制 zip 需改为源码重提 | 不落地 |

- 来源映射：`.cherry-pick-map.json`（#1 84 文件、#4 37 文件）
- 评审报告：`源文件/PR评审报告/2026-07-19.md`
- GitHub 文案：`docs/superpowers/specs/2026-07-19-pr-close-messages.md`
- 回归：PR #1、#4 验证通过；平台 lint/build 通过
- GitHub close/评论不属于本轮 Task 12；待 `gh` 凭据与协作者权限有效后执行
- 下轮同步先按 spec §11 判断 PR 状态，再决定是否覆盖本地内容
- 本地提交序列：baseline -> PR #4 -> PR #1 -> Task 12 docs
